# {{ spec.project.name }} — Project Specification

> Generated by [Archon](https://github.com/archon-ai/archon) · Schema v{{ spec.schema_version }} · {{ spec.generated_at.strftime('%Y-%m-%d') }}

---

## Overview

**{{ spec.project.description }}**

| Field | Value |
|-------|-------|
| Project Type | {{ spec.project.project_type.label }} |
| Consumer Scale | {{ spec.project.consumer_scale.label }} |
| Dev Scale | {{ spec.project.dev_scale.label }} |
| Architecture Style | {{ spec.architecture.architecture_style }} |
| Developer Expertise | {{ spec.project.expertise_level.label }} |

---

## The Magic Moment

{% if spec.phase2.magic_moment %}
> {{ spec.phase2.magic_moment }}
{% else %}
> *(not captured)*
{% endif %}

This is the single user experience this project must nail. Every architectural decision
should be evaluated against whether it enables or hinders this moment.

---

## Core Features

{% for feature in spec.phase2.core_features %}
### {{ loop.index }}. {{ feature.name }} `[{{ feature.priority.value.replace('_', '-') }}]`

{{ feature.description }}

{% endfor %}
{% if not spec.phase2.core_features %}
*(no features captured)*
{% endif %}

---

## Agentic Tools

{% for tool in spec.project.agentic_tools %}
{% set tier = spec.project.get_subscription(tool) %}
- **{{ tool.label }}**{% if tier %} — {{ tier.label }}{% endif %}

  *Best for: {{ tool.strengths | join(', ') }}*
{% endfor %}

{% if spec.project.distribute_across_agents %}
> Tasks will be distributed across the tools above. See `ROADMAP.md` for the
> agent assignment matrix.
{% else %}
> All tasks will be handled by **{{ spec.project.primary_tool.label }}**.
{% endif %}

---

## Constraints & Context

{% set c = spec.phase2.constraints %}
| Constraint | Value |
|------------|-------|
| Compliance | {{ c.compliance | map(attribute='value') | join(', ') | upper or 'None' }} |
| Existing codebase | {{ 'Yes' if c.existing_codebase else 'No (greenfield)' }} |
| Budget | {{ c.budget_range or 'Not specified' }} |
| Timeline | {{ c.timeline or 'Not specified' }} |

{% if c.key_constraints %}
**Hard constraints:**
{% for constraint in c.key_constraints %}
- {{ constraint }}
{% endfor %}
{% endif %}

{% if c.existing_codebase and c.existing_stack_notes %}
**Existing stack notes:** {{ c.existing_stack_notes }}
{% endif %}

---

## Interview Summary

{{ spec.phase2.interview_summary }}

---

*This file is the human-facing overview. For technical details see `ARCHITECTURE.md`,
for the development plan see `ROADMAP.md`, and for decision rationale see `DECISIONS.md`.*
