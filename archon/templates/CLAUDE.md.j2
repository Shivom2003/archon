# CLAUDE.md — {{ spec.project.name }}

> Auto-generated by [Archon](https://github.com/archon-ai/archon). Do not edit manually.
> Source of truth: `.architect/` directory.
> Regenerate with: `archon export --format claude-md`

---

## Project

**{{ spec.project.name }}** — {{ spec.project.description }}

**Architecture:** {{ spec.architecture.architecture_style }} · **Scale:** {{ spec.project.consumer_scale.label }}

---

## Tech Stack

{% if spec.architecture.tech_stack.languages %}Languages: {{ spec.architecture.tech_stack.languages | join(', ') }}{% endif %}

{% if spec.architecture.tech_stack.frameworks %}Frameworks: {{ spec.architecture.tech_stack.frameworks | join(', ') }}{% endif %}

{% if spec.architecture.tech_stack.databases %}Databases: {{ spec.architecture.tech_stack.databases | join(', ') }}{% endif %}

{% if spec.architecture.tech_stack.infra_cloud %}Infrastructure: {{ spec.architecture.tech_stack.infra_cloud | join(', ') }}{% endif %}

---

## System Components

{% for component in spec.architecture.components %}
**{{ component.name }}** (`{{ component.technology or 'TBD' }}`): {{ component.description }}
{% if component.responsibilities %}Handles: {{ component.responsibilities | join('; ') }}{% endif %}

{% endfor %}

---

## What I'm Building

{{ spec.phase2.interview_summary }}

**The magic moment:** {{ spec.phase2.magic_moment or 'Not specified' }}

---

## Core Features

{% for feature in spec.phase2.core_features %}
- `[{{ feature.priority.value.upper() }}]` **{{ feature.name }}**: {{ feature.description }}
{% endfor %}

---

## Constraints

{% set c = spec.phase2.constraints %}
{% if c.compliance %}Compliance: {{ c.compliance | map(attribute='value') | join(', ') | upper }}{% endif %}
{% if c.timeline %}Timeline: {{ c.timeline }}{% endif %}
{% if c.budget_range %}Budget: {{ c.budget_range }}{% endif %}
{% if c.key_constraints %}
Hard constraints:
{% for constraint in c.key_constraints %}
- {{ constraint }}
{% endfor %}
{% endif %}

---

## Key Architectural Decisions

{% for adr in spec.decisions %}
**ADR-{{ '%03d' % adr.number }}: {{ adr.title }}** — {{ adr.decision }}
*Why:* {{ adr.rationale | truncate(200) }}

{% endfor %}

---

## Development Plan

See `.architect/ROADMAP.md` for the full task breakdown with checkpoints.

{% if spec.project.distribute_across_agents %}
Tasks are distributed across: {{ spec.project.agentic_tools | map(attribute='label') | join(', ') }}.
{% endif %}

**Important:** Read `.architect/ROADMAP.md` before starting any phase to understand
checkpoint markers — these tell you when to pause and suggest a resume prompt for
the next session.

---

## Do Not

{% set c = spec.phase2.constraints %}
{% if c.key_constraints %}
{% for constraint in c.key_constraints %}
- Violate constraint: "{{ constraint }}"
{% endfor %}
{% endif %}
{% if c.compliance %}
- Skip {{ c.compliance | map(attribute='value') | join('/') | upper }} compliance requirements
{% endif %}
- Deviate from the `{{ spec.architecture.architecture_style }}` architecture without updating `DECISIONS.md`
- Ignore checkpoint markers in `ROADMAP.md` — they exist to protect usage limits
