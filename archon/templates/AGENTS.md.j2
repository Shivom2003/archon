# AGENTS.md — {{ spec.project.name }}

> Auto-generated by [Archon](https://github.com/archon-ai/archon). Do not edit manually.
> Source of truth: `.architect/ARCHITECTURE.md` and `.architect/SPEC.md`.
> Regenerate with: `archon export --format agents-md`

---

## Project

**{{ spec.project.name }}** — {{ spec.project.description }}

- **Type:** {{ spec.project.project_type.label }}
- **Architecture:** {{ spec.architecture.architecture_style }}
- **Scale:** {{ spec.project.consumer_scale.label }} (consumers) · {{ spec.project.dev_scale.label }} (team)

---

## Build & Test

> Fill in actual commands once the project is bootstrapped.
> Archon generates the structure — you add the commands.

```bash
# Install dependencies
# <fill in>

# Run tests
# <fill in>

# Lint
# <fill in>

# Build / compile
# <fill in>
```

---

## Tech Stack

{% if spec.architecture.tech_stack.languages %}
- **Languages:** {{ spec.architecture.tech_stack.languages | join(', ') }}
{% endif %}
{% if spec.architecture.tech_stack.frameworks %}
- **Frameworks:** {{ spec.architecture.tech_stack.frameworks | join(', ') }}
{% endif %}
{% if spec.architecture.tech_stack.databases %}
- **Databases:** {{ spec.architecture.tech_stack.databases | join(', ') }}
{% endif %}
{% if spec.architecture.tech_stack.infra_cloud %}
- **Infrastructure:** {{ spec.architecture.tech_stack.infra_cloud | join(', ') }}
{% endif %}

---

## Architecture

**Style:** {{ spec.architecture.architecture_style }}

### Components

{% for component in spec.architecture.components %}
- **{{ component.name }}** (`{{ component.technology or 'TBD' }}`): {{ component.description }}
{% endfor %}

---

## Key Constraints

{% set c = spec.phase2.constraints %}
{% if c.compliance %}
- **Compliance:** {{ c.compliance | map(attribute='value') | join(', ') | upper }}
{% endif %}
{% if c.key_constraints %}
{% for constraint in c.key_constraints %}
- {{ constraint }}
{% endfor %}
{% endif %}
{% if not c.compliance and not c.key_constraints %}
- No hard constraints recorded.
{% endif %}

---

## Architectural Decisions

For full rationale, see `.architect/DECISIONS.md`. Key decisions:

{% for adr in spec.decisions[:5] %}
- **ADR-{{ '%03d' % adr.number }}** {{ adr.title }}: {{ adr.decision | truncate(120) }}
{% endfor %}
{% if spec.decisions | length > 5 %}
- *({{ spec.decisions | length - 5 }} more decisions in DECISIONS.md)*
{% endif %}

---

## Development Conventions

> Add your project-specific conventions here after bootstrapping.
> Examples: branch naming, commit message format, PR review requirements.

---

## Agent Notes

{% if spec.project.distribute_across_agents %}
This project uses **multi-agent task distribution**. See `.architect/ROADMAP.md`
for the agent assignment matrix — each phase specifies which tool to use.
{% else %}
Primary development agent: **{{ spec.project.primary_tool.label }}**.
{% endif %}

- Full architecture spec: `.architect/ARCHITECTURE.md`
- Development roadmap with checkpoints: `.architect/ROADMAP.md`
- Decision rationale: `.architect/DECISIONS.md`
